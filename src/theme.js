import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#806fff',
      light: 'skyblue',
    },
    secondary: {
      main: '#332C66',
    },
    otherColor: {
      main: '#999',
    },
  },
});
